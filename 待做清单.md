# Aster 待做清单 (Backlog)

本清单系统性整理待开发的优化方案与产品演进项，重点对标 **Surge** 的测速交互与网络感知体验，并深度借鉴 **Sparkle (Mihomo GUI)** 在网络治理、连接诊断、免服务 TUN 与配置覆写方面的优秀实践，持续提升客户端的响应速度、操作友好度与工程健壮性。

---

## 一、 对标 Surge 测速与交互友好化改造专题

### 阶段一：节点交互友好化与即时动效 (UI/UX 体验重构 - P0)

- [x] **1.1 节点卡片局部独立测速加载态 (`LatencyBadge`)**
  - **背景现状**：目前点击单节点测速或一键测速时，仅顶栏大按钮置灰，卡片上的延时胶囊无独立 Loading 动画，用户无法直观感知具体节点测速中状态。
  - **改进方案**：
    - 在 `AsterState` 中引入并维护 `testingTags: Set<String>`（正在测速的节点集合）；
    - 单节点测速时，对应卡片右上角的延时胶囊立即转换为微型旋转动画（`ProgressView.controlSize(.small)`）；
    - 测速完成或超时时，动画无缝切换为对应的绿/黄/红延时数字或超时徽章。
  - **涉及文件**：
    - `macos-native/Sources/Aster/AsterAPI.swift`
    - `macos-native/Sources/Aster/UIComponents.swift`
    - `macos-native/Sources/Aster/Views.swift`

- [x] **1.2 一键测速全量并发加载态与逐个点亮机制**
  - **背景现状**：一键测速时用户需面对全局静态等待，缺乏渐进式反馈。
  - **改进方案**：
    - 点击“一键全部测速”后，所有待测节点徽章统一进入加载动效；
    - 随着测速进程推进，已测完的节点即时落定延时，呈现“逐一破晓点亮”的灵动视觉，大幅降低等待焦虑感。
  - **涉及文件**：`macos-native/Sources/Aster/AsterAPI.swift`

- [x] **1.3 节点智能排序系统 (最低延迟优先 / 超时沉底)**
  - **背景现状**：节点列表完全依附订阅文件的固有顺序排列，节点较多时用户需要肉眼寻找低延时绿色节点。
  - **改进方案**：
    - 节点管理顶部工具栏新增分段控制器：`[默认顺序 | 最低延迟优先]`；
    - 选中“最低延迟优先”时，按以下优先级动态升序排列：
      1. 已测通节点按 `delayMs` 从低到高升序排列（绿色低延时优先置顶）；
      2. 未测速节点（`delayMs == 0`）排在中间；
      3. 超时或异常节点（`delayMs < 0`）自动沉底置后。
  - **避坑要点**：测速进行中禁止频繁动态重排，避免界面跳动导致用户误触；仅在测速全部结束或用户主动切换排序方式时触发动画平滑重排。
  - **涉及文件**：`macos-native/Sources/Aster/Views.swift`

- [x] **1.4 悬停微交互与单点测速触达优化**
  - **改进方案**：
    - 鼠标悬停（Hover）在节点卡片上时，卡片右上角显现微型刷新图标（`↻`），支持一键单测；
    - 卡片激活选中态边框与阴影过度更柔和，支持触觉（Haptic）差异化反馈。
  - **涉及文件**：`macos-native/Sources/Aster/Views.swift`

---

### 阶段二：探测引擎与多探针高可用 (底层网络架构 - P1)

- [x] **2.1 多探针靶点池与智能容灾 (Delay Target Pool)**
  - **背景现状**：目前硬编码单一的 Google 探针 `https://www.gstatic.com/generate_204`，若某些机场屏蔽 Google 或特定地区对 Google DNS 污染，容易导致测速误判。
  - **改进方案**：
    - 建立多探针高可用池（Google 204 / Cloudflare 204 / Apple Captive / 自定义）；
    - 设置中开放“测速探针靶点池与自定义 URL”选项，支持用户针对特定网络环境配置专属探针；
    - 首选探针故障时自动向备选探针容灾复测。
  - **涉及文件**：
    - `internal/state/state.go`
    - `internal/app/status.go`
    - `internal/render/render.go`
    - `macos-native/Sources/Aster/Views.swift` (SettingsView)

- [x] **2.2 测速熔断时间动态调优**
  - **背景现状**：当前单节点超时时间为 5000ms（5秒），遇到多个坏节点时整体批处理等待时间偏长。
  - **改进方案**：
    - 将默认主动探测超时调整为 2500ms~3000ms（对标 Surge 的短平快熔断机制）；
    - 快速淘汰不可用节点，全量测速总耗时缩短 40% 以上。
  - **涉及文件**：`internal/app/status.go`

- [x] **2.3 渐进式流式回调 (Streaming Delay Updates)**
  - **背景现状**：Go 守护进程在 `DelayMany` 全部测完后才一次性返回 HTTP 响应。
  - **改进方案**：
    - 后端每测完一个节点，立即通过 WebSocket 发送 `{"type":"node_delay", "tag":"...", "delay":123}` 微型消息；
    - 前端实时接收并增量写入，达成真正的毫秒级流式更新。
  - **涉及文件**：
    - `internal/app/status.go`
    - `internal/app/hub.go`
    - `macos-native/Sources/Aster/AsterAPI.swift`

---

### 阶段三：高级网络感知与诊断工具 (进阶对标 Surge - P2)

- [x] **3.1 真实流量被动采样 (Passive Latency Piggybacking)**
  - **机制原理**：参考 Surge 核心优势，在用户日常产生实际代理连接时，由守护进程在底层静默抓取建连与握手 RTT，经滑动窗口 EMA 指数加权移动平均滤波自动平滑刷新该节点延时。
  - **用户体感**：用户无需手动点击测速，正在使用的活跃节点延时会伴随日常上网自动保持最新。
  - **涉及文件**：`internal/app/app.go` (`pollSnapshot` & `passiveSamplePing`)

- [x] **3.2 独立带宽与峰值吞吐测速面板 (Bandwidth Speed Test)**
  - **机制原理**：不仅测试往返握手延迟，针对重点节点提供单次真实大文件（10MB CDN 切片）真实下载测试，统计真实传输耗时并计算实际下行峰值带宽（Mbps）。
  - **用户体感**：卡片右键菜单与悬停操作均可一键启动峰值测速，测完后卡片展示 `⚡️ 85.4 M` 吞吐徽章。
  - **涉及文件**：
    - `internal/app/speedtest.go`
    - `internal/app/status.go`
    - `internal/api/server.go`
    - `macos-native/Sources/Aster/Views.swift` (NodeCardView)

---

## 二、 借鉴 Sparkle (Mihomo GUI) 架构与高级功能专题

### 阶段四：网络治理与极客诊断 (借鉴 Sparkle - P0/P1)

- [x] **4.1 活跃连接诊断与右键一键加规则 (Connections Quick Rule Injection - P0)**
  - **借鉴来源**：Sparkle 极其强大的 `connections.tsx` 连接审计页。
  - **改进方案**：
    - 在连接详情列表展示：发起进程名、图标、PID、目的 Host/IP、命中规则、链式出站与上下行流量；
    - 支持单击/右击任意活跃连接，弹出“快捷添加规则”菜单（一键设定为 DIRECT / PROXY / REJECT），无需手动复制域名输入规则表单。
  - **涉及文件**：
    - `macos-native/Sources/Aster/Views.swift` (ConnectionsAndLogsView)
    - `macos-native/Sources/Aster/AsterAPI.swift`

- [x] **4.2 按需原生管理员授权的 TUN 生命周期 (P0)**
  - **设计取舍**：不采用 Sparkle 的 SUID 方案，也不安装常驻特权 helper。
  - **机制**：每次启动或停止需要 TUN 的 sing-box 实例时，由 App 发起 macOS 原生管理员授权；取消授权、端口冲突或 TUN 建立失败均保留旧运行态并清理系统代理残留。
  - **安全边界**：不执行 `chown root:wheel`、不设置 `chmod 4755`，不改变下载内核的所有者或权限模型。
  - **涉及文件**：`internal/macos/macos.go`、`internal/core/core.go`、`internal/app/power.go`、`macos-native/Sources/Aster/ViewsSettings.swift`

- [x] **4.3 原生毛玻璃桌面网速悬浮窗 (Native Floating Traffic Monitor - P1)**
  - **借鉴来源**：Sparkle `FloatingApp.tsx` 桌面悬浮窗。
  - **原生超越**：
    - Sparkle 基于 Electron 渲染悬浮窗占用内存较大（~100MB+）；
    - 我们使用 macOS 原生 `NSPanel`（属性配置为 `.nonactivatingPanel` + `level = .floating`），构建超轻量（<1MB 内存开销、0% CPU）半透明毛玻璃悬浮网速微件；
    - 悬浮窗置顶显示实时上下行速率、当前节点国旗/名称，支持点击快速切换分流模式与快捷隐藏。
  - **涉及文件**：
    - `macos-native/Sources/Aster/FloatingPanel.swift`
    - `macos-native/Sources/Aster/AsterApp.swift`

- [x] **4.4 轻量订阅清洗与正则过滤引擎 (Lightweight Sub Cleaner - P1)**
  - **借鉴来源**：Sparkle 深度集成 Sub-Store 的订阅清洗理念。
  - **原生自研**：
    - 不引入庞大的 Node.js 运行时，直接在 Go 后端（`internal/sub/clean.go`）内置高频订阅清洗管道；
    - 提供常用清洗能力：自动识别并剔除机场公告/剩余流量等假节点、清洗广告后缀、正则排除过滤。
  - **涉及文件**：`internal/sub/clean.go`、`internal/app/subs.go`

---

### 阶段五：配置持久化与云端生态 (借鉴 Sparkle - P2)

- [x] **5.1 规则持久化覆写层 (Persistent Rule & DNS Override Pipeline - P2)**
  - **借鉴来源**：Sparkle `src/main/core/factory.ts` 的配置覆写流水线。
  - **痛点解决**：机场订阅更新往往会冲掉用户自定义的路由策略或 DNS 配置。
  - **改进方案**：
    - 构建清晰的配置生成流水线：`订阅源节点 ➔ 广告清洗 ➔ 用户持久化规则覆写层 (Override) ➔ 生成最终工作配置`；
    - 用户的自定义规则集、分流策略锁定为最高优先级覆写，无论订阅如何更新均不丢失。
  - **涉及文件**：`internal/render/render.go`、`internal/state/state.go`

- [x] **5.2 iCloud Drive / WebDAV 配置多端同步与备份 (Cloud Sync & Backup - P2)**
  - **借鉴来源**：Sparkle 支持 WebDAV 与 GitHub Gist 备份恢复。
  - **原生方案**：
    - 原生支持 macOS **iCloud Drive 零配置同步**（自动保存在用户 iCloud 容器 `~/Library/Mobile Documents/com~apple~CloudDocs/Aster/`，无感安全同步）；
    - 设置中提供 Apple ID 状态识别、一键备份至 iCloud 与二次确认安全恢复。
  - **涉及文件**：
    - `internal/app/status.go`
    - `internal/api/server.go`
    - `macos-native/Sources/Aster/Views.swift` (SettingsView)

---

### 阶段六：用户体验极致打磨与精细化定制 (四项核心体验闭环 - P0)

- [x] **6.1 主窗口初始化默认尺寸与自适应记忆 (`AsterMainWindowAutosave`)**
  - **问题痛点**：默认启动时窗口偏矮导致概览卡片产生垂直下拉滚动条；用户调节后重启重置。
  - **解决方案**：
    - 主窗口默认尺寸升级为 **980 x 760**，保证 Dashboard 所有卡片完整呈现在单屏之内，零滚动条；
    - 绑定 AppKit 原生 `setFrameAutosaveName("AsterMainWindowAutosave")`，用户任何调整后的宽高与屏幕位置由系统自动持久化记忆并无缝还原。
  - **涉及文件**：
    - `macos-native/Sources/Aster/AsterApp.swift`
    - `macos-native/Sources/Aster/Views.swift`

- [x] **6.2 代理出口 IP 优雅占位与异常容灾 (`---` 占位符)**
  - **问题痛点**：切换直连模式或无可用代理时，原逻辑显示刺眼的“检测失败”，给用户造成故障错觉。
  - **解决方案**：
    - 模式为 `direct` 时直接展示 `---` 与 `直连模式 (不经代理)`；
    - 内核未运行或未开启时展示 `---` 与 `未启用代理`；
    - 探测无响应或无出口时优雅展示 `---` 与 `暂无可用出口`，彻底消除所有红色“检测失败”文字。
  - **涉及文件**：
    - `internal/app/app.go`
    - `macos-native/Sources/Aster/Views.swift`

- [x] **6.3 偏好设置全功能交互开关定制 (UI & Features)**
  - **问题痛点**：新增的微交互与辅助小功能缺乏用户自主配置开关。
  - **解决方案**：
    - 在“偏好设置”中设立显式的**“界面交互与功能定制”**独立卡片；
    - 纳管 5 大可持久化开关：桌面实时网速悬浮窗、节点卡片峰值吞吐 (⚡️Mbps) 徽标、节点列表默认按延迟升序排序、活跃连接快捷注入规则入口、日常流量被动伴随采样 (Surge 机制)。
  - **涉及文件**：
    - `internal/app/app.go`
    - `internal/api/server.go`
    - `macos-native/Sources/Aster/AsterAPI.swift`
    - `macos-native/Sources/Aster/Views.swift`

- [x] **6.4 状态栏现代 NSPopover 架构面板 (精简命名 + 原地测速保留显示)**
  - **核心痛点**：原生 `NSMenu` 只要一点击测速项，底层 Carbon 追踪事件立即强行关闭菜单，导致用户根本看不到测速过程与延迟结果；菜单文字啰嗦。
  - **解决方案**：
    - 架构全面升级为现代 **`NSPopover`**（`StatusPopoverController` + `StatusMenuPopoverView`）；
    - 所有功能命名精简贴合职责：`打开主界面`、`桌面悬浮窗`、`系统代理`、`TUN 模式`、`规则分流`、`全局代理`、`直接连接`、`全部测速`、`重启内核`、`退出`；
    - **原地交互保留**：点击“全部测速”面板绝不关闭，微型 Spinner 原地旋转并结合 WebSocket 逐个流式点亮节点最新延迟与吞吐；用户点击外部区域时随手自动关闭；右键保留极速快捷菜单。
  - **涉及文件**：
    - `macos-native/Sources/Aster/StatusPopover.swift`
    - `macos-native/Sources/Aster/AsterApp.swift`
    - `scripts/build_aster_mac.sh`
    - `Makefile`

---

### 阶段七：Surge for Mac 4 大核心视窗全量复刻专题 (全景美学与生产力跃迁 - 100% 达成)

- [x] **7.1 视窗一：主控制台「活动 (Activity)」Bento 仪表盘 (1:1 像素级复刻截图 1)**
  - **顶栏网络拓扑**：
    - 大标题 `活动` + 异常状态微标告警；
    - 右上角可点击即时切换的接管状态胶囊：`系统代理`（绿/灰点）+ `增强模式`（绿/灰点）；
    - 4 列网络元数据横栏：`网络`（以太网/Wi-Fi 图标）、`配置`（自用维护配置）、`出站模式`（智能规则 ⌵ / 直接连接 ⌵ / 全局代理 ⌵ 下拉即切）、`外部 IP`（国家旗帜 + 外部出口 IP，点击 Popover 弹出本机真实 IP vs 代理出口 IP 对比详情与复制按钮）。
  - **Bento Grid 六大核心监控卡片**：
    1. **INTERNET 延迟卡片**：绿色大字延时（如 `45 ms` 或 `≤1 ms`） + `[网络诊断]` 异步探针按钮 + 底部三级延时（`路由 ≤4 ms` | `DNS 2 ms` | `代理 45 ms / 不适用`）；
    2. **上传吞吐卡片**：紫色大字瞬时速率（`0 B/s`） + 标尺（`131 KB/s`） + 底部紫色平滑动态示波进度条；
    3. **下载吞吐卡片**：青蓝色大字瞬时速率（`0 B/s`） + 标尺（`131 KB/s`） + 底部青蓝色平滑动态示波进度条；
    4. **活动连接卡片**：大字并发连接数 + 呼吸绿灯 + 底部 `X 进程 · 1 设备 · 0 DHCP 设备`；
    5. **流量时序示波器卡片**：`[全部 | 仅代理]` 切换 + 24 小时微型时序柱状图（`12AM` → `6AM` → `12PM` → `6PM`） + `[进程与设备 | 域名 | 策略]` 维度分段器；
    6. **周期累计流量卡片**：`[今日 | 本月]` 分段器 + 大字总流量 + 紫色（`DIRECT`）与青色（`代理`）双色比例条与图例。

- [x] **7.2 视窗二：主控制台「概览 (Overview)」网络接管控制中心 (1:1 像素级复刻截图 2)**
  - **网络接管卡片池**：
    - `系统代理`：网络大圆标、状态指示（`🟢 已配置为 127.0.0.1:2080 (HTTP / HTTPS)`）、说明文案、Toggle 开关、`•••` 更多操作（复制终端代理命令、测试连接）；
    - `增强模式 (TUN)`：芯片大圆标、状态指示（`🟢 运行中 (utun3 · 198.18.0.1/16)`）、说明文案、Toggle 开关。
  - **局域网设备接管卡片池**：
    - `HTTP & SOCKS5 代理共享`：局域网大圆标、状态指示（`🟢 HTTP 监听在 192.168.x.x:2080 · SOCKS5: 2080`）、说明文案、局域网共享 Toggle 开关、`•••` 端口配置弹层；
    - `网关模式 (DHCP 接管)`：路由器大圆标、引导文案、`配置向导…` 按钮。
  - **远程连接与漫游卡片池**：
    - `设备漫游与远程控制`：iCloud 云端大圆标、状态指示（`已就绪`）、`管理配置` 按钮。

- [x] **7.3 视窗三：状态栏交互菜单 (1:1 像素级复刻截图 3)**
  - **顶部系统控制**：`显示主窗口 (⌘M)`；
  - **出站模式徽标**：右侧蓝色状态徽标 `[R]`（Rule）/ `[G]`（Global）/ `[D]`（Direct），点击原地展开三模式单选子项；
  - **快捷规则**：`为当前网页设置规则…`；
  - **策略组与节点**：显示当前活跃出站节点、带宽吞吐徽章（`⚡️ 85M`）、延时指示灯，支持原地展开切换节点与测速；
  - **全部测速**：点击原地保留，逐个流式点亮延时；
  - **🔥 活跃进程与客户端实时速率 Top 榜**：
    - 菜单直接列出当前活跃传输的前 3~5 个 App 进程；
    - 自动提取 macOS 本地应用程序高清晰图标（如 `钉钉`、`ChatGPT`、`Cursor`、`Chrome` 等）并显示瞬时网速（如 `0 B/s` 或 `12.4 KB/s`）；
  - **🔥 独立面板入口与快捷命令**：
    - `Surge 面板… (⌘D)`：一键呼出独立抓包分析视窗；
    - `✓ 设置为系统代理 (⌘S)`；
    - `增强模式 (⌘E)`；
    - `🔥 复制终端代理命令 (⌘C)`：一键将 `export http_proxy=...` 拷贝至剪贴板；
    - `重载配置 (⌘R)`；
    - `退出 (⌘Q)`。

- [x] **7.4 视窗四：独立「Surge 面板」抓包分析视窗 (Inspector - 1:1 像素级复刻截图 4, 快捷键 ⌘D)**
  - **独立 AppKit 窗口体系**：单例 `InspectorWindowController`，支持全局快捷键 `⌘D` 与侧边栏底部 `面板 ↗` 随时唤起，自带 `AsterInspectorWindowAutosave` 位置尺寸记忆；
  - **顶部 6 大分段导航**：`[ 最近的请求 | 活动连接 | DNS | 设备 | 流量统计 | 日志簿 ]` + 右侧实时搜索过滤框；
  - **左侧客户端维度筛选**：
    - `[ 按客户端 | 按主机名 ]` 分段器；
    - 自动检索本地应用（`Chrome`、`Telegram`、`ChatGPT`、`钉钉`、`Cursor` 等）提取高清图标并统计当前请求数；
    - 点击特定 App 实时联动右侧列表仅展示该 App 的网络请求流水；
  - **右侧高密度抓包流水表**：
    - 10 列高密度字段：状态灯（🟢/🟡/🔴）、序号 ID、时间戳、客户端图标+名称、命中规则（`IP-CIDR`、`DOMAIN-SUFFIX`、`FINAL`）、出口策略、时长、彩色请求方法徽标（`HTTPS` 橙黄色、`POST` 蓝色、`TCP` 绿色、`UDP` 紫色）、目标地址与端口；
  - **底部专业操作工具栏**：`清空`、`重新载入`、`启动流量捕获`、`开启 MitM`、`代理控制`。

- [x] **7.5 支撑引擎全量闭环 (Go Daemon + Swift 桥接)**
  - **三级延时探测引擎 (`internal/app/diagnostics.go`)**：并发探测网关 ICMP/TCP 握手延时、系统 DNS 递归解析延时、当前代理节点往返耗时；
  - **进程流量聚合器 (`internal/app/process_stats.go`)**：从连接池实时聚合进程瞬时速率，提取 Top 活跃应用；
  - **原生应用图标缓存 (`iconCache`)**：支持 `/Applications` 与 `/System/Applications` 模糊匹配，毫秒级缓存原生图标。

```
      [ 阶段一：UI/UX 友好化 (Surge 对标) ] (P0)
       ├── 1.1 节点卡片局部 Loading 转圈
       ├── 1.2 一键测速逐个流式点亮
       ├── 1.3 节点智能排序 (最低延迟置顶/超时沉底)
       └── 1.4 卡片悬停微交互
                │
                ▼
      [ 阶段四：网络治理与极客诊断 (Sparkle 借鉴) ] (P0/P1)
       ├── 4.1 连接诊断一键加规则 (直连/代理/拦截)
       ├── 4.2 按需原生管理员授权 TUN 生命周期
       ├── 4.3 原生毛玻璃桌面网速悬浮窗 (NSPanel)
       └── 4.4 轻量订阅清洗与正则过滤 (Go 原生)
                │
                ▼
      [ 阶段二：底层探针与高可用 ] (P1)
       ├── 2.1 多靶点探针池 (Google / Cloudflare / Apple)
       ├── 2.2 2500ms 快速超时熔断
       └── 2.3 WebSocket 节点延时渐进推送
                │
                ▼
      [ 阶段三 & 五：高级感知与云端生态 ] (P2)
       ├── 3.1 真实流量被动伴随采样 (EMA)
       ├── 3.2 真实带宽吞吐测速面板 (Mbps)
       ├── 5.1 规则持久化覆写层 (Override Pipeline)
       └── 5.2 iCloud Drive / WebDAV 多端备份
```

---

## 五、工程化改造状态（2026-09）

下列工程债已在「彻底工程化」批次中处理，不再作为产品功能勾选：

- 数据目录：`~/Library/Application Support/Aster/`
- 去掉本机硬编码 daemon 路径；控制面 Bearer token
- Status 持锁 IO 修复；内核崩溃退避重启；daemon.pid / daemon.log
- StatusPopover 左键接线；设置项与后端同步（strictRoute / allowLan / autostart / 测速参数）
- Views 拆分；Xcode 工程；CI；开源 README / LICENSE / CONTRIBUTING
- 可复现打包（vendor sing-box，不再读用户 Application Support）
- 移除未接后端的延迟历史与自动回退占位开关，避免虚假的已启用状态


1. **界面交互无撕裂**：测速与悬浮窗运行期间，界面流畅度保持在 60fps/120fps，卡片列表无剧烈晃动，点击响应无卡顿。
2. **状态语义清晰**：
   - 加载中：旋转 ProgressView 徽章；
   - 成功：绿/黄/红毫秒级徽章；
   - 超时：珊瑚红“超时”徽章；
   - 未测：灰色“---”徽章。
3. **极简轻量开销**：
   - 客户端常驻内存必须严格控制在 **30MB ~ 60MB** 以内（拒绝 Electron 级百兆膨胀）；
   - 桌面悬浮窗空闲 CPU 占用严格保持在 **0% ~ 0.5%**。
4. **Fail-Safe 容灾机制**：无论网络断开、超时还是订阅节点全挂，测速与规则注入均能平稳收尾，不可导致 UI 线程阻塞或后台进程崩溃。
