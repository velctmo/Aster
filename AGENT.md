# AGENT.md - Aster 核心开发规则与智能体行为准则

> **最高宪章**：本文档为 Aster 项目的最高开发规范与行为约束指南。所有参与本项目的 AI 智能体与开发者必须严格遵守以下纯规则与准则。

---

## 一、 数据真实性与反伪造准则 (Truthfulness & Zero-Fabrication)

1. **绝对忠实于物理真实状态**
   - 严禁任何形式的数据伪造、盲目默认兜底与“为防报错而掩盖真相”。
   - 当探测超时、网络离线、服务未启动或权限缺失时，状态必须如实向上传递并暴露，严禁静默重置为成功值（如测速失败重置为 0）或捏造虚假设备与本地地址。
2. **严禁凭空捏造系统元素**
   - 严禁在代码、注释或界面展示中引入未定义、不存在或未经真实测试验证的规则集、内置预设或系统配置。

---

## 二、 环境隔离与测试纯洁性规范 (Environment-Agnostic Testing)

1. **纯逻辑测试与底层系统强隔离**
   - 单元测试必须具备环境无感知性。验证数据解析、模型映射、配置转换及内存状态逻辑时，严禁隐式触发需要外部二进制或底层守护进程的物理启动流程。
   - 涉及外部进程依赖的流程测试，必须在测试层提供纯净的模拟替代，严禁假设执行环境预装了特定系统工具或二进制文件。
2. **零缓存严格验证原则**
   - 严禁依赖任何带缓存的测试结果；所有自动化回归必须在无缓存、全量开启并发数据竞争检测的前提下完成。

---

## 三、 设计系统与统一抽象准则 (Design System & Anti-Fragmentation)

1. **公共组件单一权威，拒绝局部造轮子**
   - 全局界面元素（子卡片底色、描边发丝、空状态展示、提示横幅、单选药丸、设置控制行、反馈按钮等）必须严格调用项目公共设计系统。
   - 严禁在业务视图层私自拼接样式或创建平行组件。发现多处相似的视觉与交互模式时，必须先沉淀为通用基础组件，再统一引用。
2. **视窗安全与无缝视效约束**
   - 模态与独立弹窗必须全面消除安全区域穿透，确保材质完整覆没至窗口边缘，杜绝控制区与系统栏出现视觉断层或透明漏洞。
3. **尊重原生交互，禁止与用户操作对抗**
   - 严禁锁死窗口自由缩放比例，严禁在窗口调整时与用户的光标移动产生机械对抗。
   - 界面自适应遵循“物理最小硬边界守住可用性底线，弹性容器消化大屏扩展空间”的原则，小屏平滑降级，大屏居中防畸变。

---

## 四、 交互克制与领域规范 (Restraint & Domain Discipline)

1. **零侵入交互原则**
   - 复制、节点切换、测速、配置重载等高频日常操作，彻底禁止触发侵入式的全局通知横幅或屏幕级阻断弹窗。
   - 所有交互反馈必须遵循“物理触觉感知优先、就地行内微反馈优先”的原则，保障用户工作流的连续性。
2. **领域术语唯一性**
   - 系统中同一业务概念在前端展示、后端存储、API 接口与文档中必须保持严格唯一的专业命名，严禁使用粗糙黑话或分歧术语。

---

## 五、 状态机原子性与闭环保障 (State Integrity & Atomic Mutation)

1. **原子生效与安全回滚**
   - 任何涉及核心配置与运行状态的变更，必须遵循“预检校验 $\to$ 尝试应用 $\to$ 确认提交”的原子闭环。
   - 任何阶段发生校验失败或执行异常，必须立即恢复至修改前的纯净状态，严禁产生配置已修改但服务崩溃的脱节状态。
2. **展示层与控制层单向解耦**
   - 界面展示层仅负责渲染状态并提交操作意图，严禁在展示层私自拼装核心配置或推导底层网络状态。

---

## 六、 提交前质量门禁 (The Iron Gate Protocol)

在声称任务完成、创建提交或推送代码前，必须无条件执行且必须全量绿灯通过以下三重门禁：
1. **静态语法与类型系统全量检查**：确保各端工程代码 0 警告、0 报错；
2. **工程索引一致性校验**：确保所有新增源文件已完整纳入对应平台的工程工程配置文件，无遗漏编译孤岛；
3. **无缓存全量并发竞态测试**：确保跨包测试在数据竞争检测下 100% 通过。
