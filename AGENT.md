# AGENT.md - 开发规则、通用设计约束与禁止条令

> **本文档为 Aster 项目的最高开发宪章。所有 AI 智能体与开发者必须无条件执行以下全部条款。**

---

## 一、 代码工程规则 (Code Rules)

1. **单一权威实现**  
   任何功能、算法、状态映射或数据处理逻辑，全工程只能存在一套权威实现并全局复用。严禁为了局部便利创建功能相似的副本或并行逻辑。

2. **根治根因，拒绝修补**  
   修复缺陷必须追溯到数据模型、流转管道或状态机的源头。禁止打补丁式修复，禁止在调用端或展示层堆砌特判、Hack 分支或局部兜底。

3. **真实性优先**  
   系统必须绝对忠实反映底层的物理与网络真实状态。状态未就绪、失败或离线时如实向上传递并暴露给用户，禁止为了“防报错”或“界面好看”而掩盖真实状态。

4. **原子事务与状态安全**  
   所有对系统核心运行状态的修改必须具备原子性：遵循“验证 $\to$ 尝试生效 $\to$ 提交落盘”的闭环，遇到任何异常立即完整回滚，确保运行时状态与持久化状态绝对同构。

5. **严格分层与单向控制**  
   界面展示层仅负责渲染状态与向后台提交用户意图，禁止在界面层推导底层网络状态或拼装配置；业务核心层掌控全部状态真理与生命周期。

6. **物理级零死代码**  
   废弃的功能、无引用的函数、多余的字段以及无效文件必须物理删除。严禁将过时代码注释留存或保留空桩。

7. **绝对质量门禁**  
   任何代码变更在提交前，必须确保全量单元测试与并发竞态检测 100% 通过（0 race，0 failure），且前端代码保持 0 语法警告与 0 错误。

---

## 二、 通用设计约束 (General Design Constraints)

1. **审美基调**  
   必须呈现专业成熟、新颖大胆、精致大气的顶级原生生产力质感。彻底摒弃廉价纯色块、粗暴高对比度与粗劣阴影等 AI 生成常见模板套路。

2. **材质与曲率系统**  
   - 界面容器统一采用高通透液态/曜石磨砂玻璃材质，与系统原生桌面环境自然融为一体；
   - 所有卡片与控件必须使用几何连续曲率（Continuous Curvature），大卡片固定为 12pt，控件固定为 6pt；
   - 边框必须采用双层微发丝内描边（暗色采用微光白发丝，亮色采用金属凝霜描边），以实现精密实体硬件质感。

3. **色彩与动效约束**  
   - 实时网络数据监控必须采用低饱和度的莫兰迪专业色标（沉稳青蓝与内敛浅紫），严禁使用高饱和度刺眼荧光绿或原色蓝；
   - 核心状态指示采用柔和低频的呼吸脉冲微光信标；
   - 波形示波器必须采用平滑贝塞尔曲线插值与微透明渐变填充。

4. **交互克制规范**  
   - 交互反馈遵循物理触觉（轻触震动）优先与就地微动画优先；
   - 状态或异常提醒必须集成在卡片与列表内部的行内胶囊中呈现，严禁阻断用户的操作心流。

5. **领域术语规范**  
   - 系统初始默认配置文件规范统一命名为 **`Default`**；
   - 组合订阅与聚合集合的规范专业术语为 **“节点聚合”**，严禁使用粗糙黑话。

---

## 三、 绝对禁止条令 (Strict Prohibitions / Red Lines)

- [ ] **【严禁】瞎兜底与虚假数据**  
  - 严禁将测速超时重置为 0（超时必须返回 -1，0 仅代表未测速）；
  - 严禁在未获取真实 IP 或断网时硬编码兜底为 `127.0.0.1`；
  - 严禁在无网络时伪造以太网或活跃网卡；
  - 严禁在界面展示虚构或不存在的系统规则。

- [ ] **【严禁】功能打补丁与逻辑分叉**  
  - 严禁在不同模块各写一套节点过滤、清洗、延迟计算或模式转换逻辑；
  - 严禁为规避错误而在展示层截断或篡改后端状态数据。

- [ ] **【严禁】侵入式全局打扰**  
  - 复制、切换节点、测速、启停核心、配置切换等常规交互，严禁弹出系统通知中心横幅或屏幕级 Toast。

- [ ] **【严禁】出现“节点池”等粗糙术语**  
  - 严禁在代码、日志、异常信息或 UI 文本中出现 `"节点池"` 术语，默认配置统一为 `Default`。

- [ ] **【严禁】未经校验直接落盘状态**  
  - 严禁直接改写状态文件而不执行运行时校验与回滚保障，严禁产生“磁盘已修改但核心未启动/已崩溃”的状态脱节。

- [ ] **【严禁】配置注入导致内核崩溃**  
  - 严禁在内核配置中注入特定环境专属但会导致低版本或标准环境崩溃的未经协商字段；
  - 严禁配置无效 IPv6 FakeIP 网段导致系统网络黑洞；
  - 严禁直连 DNS 泄露境外域名明文查询。

- [ ] **【严禁】带病提交代码**  
  - 严禁在并发竞争检查存在 Warning、单元测试未完全通过或构建存在隐患的情况下声明任务完成。
